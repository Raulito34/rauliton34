# Site in preparation — kept OUT of search via <meta name="robots" content="noindex">.
# Crawling is intentionally ALLOWED so search engines can re-crawl, SEE the
# noindex tag, and drop the pages from their index. (Disallow: / would prevent
# de-indexing because the crawler could never read the noindex.)
#
# At launch, to re-enable search: remove the noindex meta from index.html
# (this robots.txt can stay as-is, or tighten Disallow to /admin/ only).

User-agent: *
Allow: /
Disallow: /admin/
